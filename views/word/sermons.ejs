<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('../partials/head') %>
        <style>
            .board-table {
                width: 100%;
                border-collapse: collapse;
                margin-top: 2rem;
                background: white;
                border-radius: 12px;
                overflow: hidden;
                box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
            }

            .board-table th {
                background: #f8f9fa;
                padding: 1.2rem;
                font-weight: 600;
                color: var(--color-text);
                border-bottom: 2px solid #edf2f7;
                text-align: left;
            }

            .board-table td {
                padding: 1.2rem;
                border-bottom: 1px solid #edf2f7;
                vertical-align: middle;
            }

            .board-table tr:hover {
                background-color: #fcfdfe;
            }

            .date-badge {
                font-size: 0.85rem;
                color: var(--color-text-light);
                background: #f1f3f5;
                padding: 4px 10px;
                border-radius: 20px;
            }

            .board-title {
                font-weight: 600;
                color: var(--color-primary);
                font-size: 1.05rem;
                display: block;
                margin-bottom: 4px;
                text-decoration: none;
            }

            .board-meta {
                font-size: 0.9rem;
                color: #666;
                display: -webkit-box;
                -webkit-line-clamp: 1;
                line-clamp: 1;
                -webkit-box-orient: vertical;
                overflow: hidden;
            }

            @media (max-width: 600px) {

                .board-table th:nth-child(2),
                .board-table td:nth-child(2) {
                    display: none;
                }
            }

            .pagination {
                display: flex;
                justify-content: center;
                align-items: center;
                gap: 0.5rem;
                margin-top: 2rem;
                padding: 1rem 0;
            }

            .pagination a,
            .pagination span {
                padding: 0.5rem 1rem;
                border: 1px solid #ddd;
                border-radius: 6px;
                text-decoration: none;
                color: var(--color-text);
                transition: all 0.3s ease;
                min-width: 40px;
                text-align: center;
            }

            .pagination a:hover {
                background: var(--color-primary);
                color: white;
                border-color: var(--color-primary);
            }

            .pagination .active {
                background: var(--color-primary);
                color: white;
                border-color: var(--color-primary);
                font-weight: 600;
            }

            .pagination .disabled {
                opacity: 0.5;
                cursor: not-allowed;
                pointer-events: none;
            }
        </style>
</head>

<body>
    <%- include('../partials/header') %>

        <section class="page-header" data-aos="fade">
            <div class="container">
                <h1 data-aos="fade-up" data-aos-delay="100">주일 설교</h1>
                <ul class="breadcrumb" data-aos="fade-up" data-aos-delay="300">
                    <li><a href="/">홈</a></li>
                    <li>말씀</li>
                    <li class="active">주일 설교</li>
                </ul>
            </div>
        </section>

        <div class="container" style="padding-bottom: var(--spacing-xl);">
            <% if (locals.user && user.isAdmin) { %>
                <div style="text-align: right; margin-bottom: 2.5rem;">
                    <a href="#" onclick="openAdminModal('/admin/write/sermons', '설교 등록')" class="btn btn-primary">설교
                        등록</a>
                </div>
                <% } %>

                    <table class="board-table transition-all" data-aos="fade-up">
                        <thead>
                            <tr>
                                <th style="width: 60%;">설교 정보</th>
                                <th style="width: 25%;">날짜</th>
                                <% if (locals.user && user.isAdmin) { %>
                                    <th style="width: 15%; text-align: center;">관리</th>
                                    <% } %>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (locals.sermons && sermons.length> 0) { %>
                                <% sermons.forEach(function(item) { %>
                                    <tr>
                                        <td>
                                            <a href="/word/sermons/<%= item.id %>" class="board-title">
                                                <i class="fas fa-video"
                                                    style="margin-right: 8px; font-size: 0.9rem; opacity: 0.7;"></i>
                                                <%= item.title %>
                                            </a>
                                            <div class="board-meta">
                                                <i class="far fa-user"></i>
                                                <%= item.preacher || '정배교회' %>
                                                    <% if(item.bibleRange) { %> | <i class="fas fa-bible"></i>
                                                        <%= item.bibleRange %>
                                                            <% } %>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="date-badge">
                                                <%= item.date %>
                                            </span>
                                        </td>
                                        <% if (locals.user && user.isAdmin) { %>
                                            <td style="text-align: center;">
                                                <div style="display: flex; gap: 10px; justify-content: center;">
                                                    <a href="#"
                                                        onclick="openAdminModal('/admin/edit/sermons/<%= item.id %>', '설교 수정')"
                                                        style="color: var(--color-secondary);"><i
                                                            class="fas fa-edit"></i></a>
                                                    <a href="/admin/delete/sermons/<%= item.id %>"
                                                        onclick="return confirm('정말 삭제하시겠습니까?')"
                                                        style="color: #e74c3c;"><i class="fas fa-trash"></i></a>
                                                </div>
                                            </td>
                                            <% } %>
                                    </tr>
                                    <% }); %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="<%= (locals.user && user.isAdmin) ? 3 : 2 %>"
                                                    class="text-center" style="padding: 5rem; color: #aaa;">
                                                    등록된 설교가 없습니다.
                                                </td>
                                            </tr>
                                            <% } %>
                        </tbody>
                    </table>

                    <% if (totalPages> 1) { %>
                        <div class="pagination">
                            <% if (currentPage> 1) { %>
                                <a href="/word/sermons?page=<%= currentPage - 1 %>">
                                    <i class="fas fa-chevron-left"></i>
                                </a>
                                <% } %>

                                    <% for (let i=1; i <=totalPages; i++) { %>
                                        <% if (i===currentPage) { %>
                                            <span class="active">
                                                <%= i %>
                                            </span>
                                            <% } else { %>
                                                <a href="/word/sermons?page=<%= i %>">
                                                    <%= i %>
                                                </a>
                                                <% } %>
                                                    <% } %>

                                                        <% if (currentPage < totalPages) { %>
                                                            <a href="/word/sermons?page=<%= currentPage + 1 %>">
                                                                <i class="fas fa-chevron-right"></i>
                                                            </a>
                                                            <% } %>
                        </div>
                        <% } %>

                            <!-- Related Action -->
                            <div style="border-top: 1px solid #eee; margin-top: 5rem; padding-top: 3rem; text-align: center;"
                                data-aos="fade-up">
                                <h4 style="margin-bottom: 2rem; color: var(--color-text-light); font-weight: 600;">다른 말씀
                                    안내 보기</h4>
                                <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                                    <a href="/word/meditation" class="btn btn-outline"
                                        style="border-radius: 50px; padding: 12px 25px;">새벽 묵상</a>
                                    <a href="/word/diary" class="btn btn-outline"
                                        style="border-radius: 50px; padding: 12px 25px;">목양 일기</a>
                                </div>
                            </div>
        </div>

        <%- include('../partials/footer') %>
            <script src="/js/main.js"></script>
</body>

</html>