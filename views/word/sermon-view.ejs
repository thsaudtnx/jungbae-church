<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('../partials/head') %>
        <style>
            .sermon-detail {
                background: white;
                padding: 3rem;
                border-radius: 15px;
                box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
                margin-top: 2rem;
            }

            .sermon-meta {
                color: var(--color-text-light);
                font-size: 1.1rem;
                margin-bottom: 2rem;
                padding-bottom: 1rem;
                border-bottom: 1px solid #eee;
            }

            .sermon-content {
                font-size: 1.15rem;
                line-height: 1.8;
                white-space: pre-wrap;
                color: #333;
            }

            .video-box {
                margin-top: 3rem;
                border-radius: 12px;
                overflow: hidden;
                background: #000;
            }

            .video-container {
                position: relative;
                padding-bottom: 56.25%;
                height: 0;
                overflow: hidden;
            }

            .video-container iframe {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
            }

            .back-btn {
                margin-top: 2rem;
                display: inline-block;
            }
        </style>
</head>

<body>
    <%- include('../partials/header') %>

        <div class="container" style="padding-top: 100px; padding-bottom: 80px;">
            <div class="section-title">
                <h1>주일 설교</h1>
            </div>

            <div class="sermon-detail">
                <h2 style="color: var(--color-text); margin-bottom: 0.5rem;">
                    <%= sermon.title %>
                </h2>
                <div class="sermon-meta">
                    <i class="far fa-calendar-alt"></i>
                    <%= sermon.date %> |
                        <i class="far fa-user"></i>
                        <%= sermon.preacher %>
                </div>

                <div class="sermon-content">
                    <%= sermon.content %>
                </div>

                <% if (sermon.videoId) { %>
                    <div class="video-box">
                        <div class="video-container">
                            <iframe src="https://www.youtube.com/embed/<%= sermon.videoId %>" frameborder="0"
                                allowfullscreen></iframe>
                        </div>
                    </div>
                    <% } %>
            </div>

            <a href="/word/sermons" class="btn btn-outline back-btn"><i class="fas fa-list"></i> 목록으로</a>
        </div>

        <%- include('../partials/footer') %>
            <script src="/js/main.js"></script>
</body>

</html>