<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('partials/head') %>
        <style>
            /* Hero Section specific styles */
            .hero {
                height: 80vh;
                background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)), url('/images/jbchurch-background.png') center/cover no-repeat;
                display: flex;
                align-items: center;
                justify-content: center;
                text-align: center;
                color: white;
                padding-top: var(--header-height);
            }

            .hero-content h1 {
                font-size: 3rem;
                margin-bottom: 1rem;
                text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
                color: white;
                line-height: 1.2;
            }

            .hero-content p {
                font-size: 1.2rem;
                margin-bottom: 2rem;
                opacity: 0.9;
            }

            @media (max-width: 768px) {
                .hero-content h1 {
                    font-size: 2.2rem;
                }

                .hero-content p {
                    font-size: 1.1rem;
                }
            }

            .quick-links {
                padding: var(--spacing-xl) 0;
                background: var(--color-bg-alt);
            }

            .card-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
            }

            .card {
                background: white;
                padding: 2rem;
                border-radius: 10px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                transition: all 0.3s ease;
                text-align: center;
                text-decoration: none;
                color: inherit;
                display: block;
                cursor: pointer;
            }

            .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            }

            .card h3 {
                color: var(--color-text);
            }

            .card p {
                color: var(--color-text-light);
            }

            .card i {
                font-size: 2.5rem;
                color: var(--color-primary);
                margin-bottom: 1rem;
            }

            .card h3 {
                font-size: 1.2rem;
                margin-bottom: 0.5rem;
            }
        </style>
</head>

<body>
    <%- include('partials/header') %>

        <!-- Hero Section -->
        <section class="hero">
            <div class="container hero-content">
                <h1>환영합니다<br>정배교회입니다</h1>
                <p>일상에 스미는 하나님 나라</p>
                <a href="/church/philosophy" class="btn btn-outline" style="border-color: white; color: white;">교회 소개
                    보기</a>
            </div>
        </section>

        <!-- Quick Links -->
        <section class="quick-links">
            <div class="container">
                <div class="card-grid">
                    <a href="/church/worship" class="card">
                        <i class="fas fa-church"></i>
                        <h3>주일 예배</h3>
                        <p>1부 9:00 / 2부 11:00</p>
                    </a>
                    <a href="https://www.youtube.com/@jeongBaeChurch" target="_blank" class="card">
                        <i class="fas fa-video"></i>
                        <h3>온라인 예배</h3>
                        <p>유튜브 실시간 방송</p>
                    </a>
                    <a href="/church/location" class="card">
                        <i class="fas fa-map-marker-alt"></i>
                        <h3>오시는 길</h3>
                        <p>경기도 양평군 서종면 중미산로 773</p>
                    </a>
                </div>
            </div>
        </section>

        <%- include('partials/footer') %>
            <script src="/js/main.js"></script>
</body>

</html>