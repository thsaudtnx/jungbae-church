<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('../partials/head') %>
        <style>
            @media (max-width: 768px) {
                .page-header {
                    padding: 3rem 0 !important;
                }

                .page-header h1 {
                    font-size: 1.8rem !important;
                }

                .container {
                    padding-top: 1rem !important;
                    padding-bottom: 2rem !important;
                }

                .card {
                    padding: 1.5rem 1rem !important;
                    border-radius: 15px !important;
                }

                .card h2 {
                    font-size: 1.45rem !important;
                    margin-bottom: 0.5rem !important;
                }

                .card p {
                    font-size: 0.9rem !important;
                    margin-bottom: 1.5rem !important;
                }

                .btn-outline,
                .btn-primary {
                    padding: 8px 18px !important;
                    font-size: 0.85rem !important;
                }

                .pdf-container {
                    aspect-ratio: 1 / 1.3 !important;
                }
            }
        </style>
</head>

<body>
    <%- include('../partials/header') %>

        <section class="page-header" data-aos="fade">
            <div class="container">
                <h1 data-aos="fade-up" data-aos-delay="100">주보 상세</h1>
                <ul class="breadcrumb" data-aos="fade-up" data-aos-delay="300">
                    <li><a href="/">홈</a></li>
                    <li>나눔</li>
                    <li><a href="/sharing/bulletin">주보</a></li>
                    <li class="active">상세보기</li>
                </ul>
            </div>
        </section>

        <div class="container" style="padding-bottom: var(--spacing-xl);">
            <div style="max-width: 900px; margin: 0 auto;">
                <div style="margin-bottom: 2rem; display: flex; justify-content: space-between; align-items: center;">
                    <a href="/sharing/bulletin" class="btn btn-outline"
                        style="border-radius: 50px; padding: 10px 25px;">
                        <i class="fas fa-arrow-left" style="margin-right: 8px;"></i> 목록으로
                    </a>
                    <% if (locals.user && user.isAdmin) { %>
                        <a href="/admin/edit/bulletins/<%= bulletin.id %>" class="btn btn-primary"
                            style="border-radius: 50px; padding: 10px 25px;">
                            <i class="fas fa-edit" style="margin-right: 8px;"></i> 수정하기
                        </a>
                        <% } %>
                </div>

                <div class="card"
                    style="padding: 2rem; border-radius: 12px; box-shadow: var(--shadow-soft); background: white;">
                    <h2 style="margin-top: 0; color: var(--color-primary);">
                        <%= bulletin.title %>
                    </h2>
                    <p style="color: #666; margin-bottom: 2rem;">
                        <i class="far fa-calendar-alt"></i>
                        <%= bulletin.date %>
                    </p>

                    <% if (bulletin.file && bulletin.file.toLowerCase().endsWith('.pdf')) { %>
                        <div class="pdf-container"
                            style="width: 100%; aspect-ratio: 1 / 1.42; margin: 0 auto; max-width: 800px; border: 1px solid #eee; border-radius: 8px; overflow: hidden;">
                            <iframe src="<%= bulletin.file %>#toolbar=0&navpanes=0&scrollbar=0&view=FitH"
                                style="width: 100%; height: 100%; border: none;" frameborder="0"></iframe>
                        </div>
                        <% } else if (bulletin.file && bulletin.file.match(/\.(jpg|jpeg|png|gif|webp)$/i)) { %>
                            <div style="width: 100%; text-align: center;">
                                <img src="<%= bulletin.file %>"
                                    style="max-width: 100%; border-radius: 8px; box-shadow: 0 5px 15px rgba(0,0,0,0.1);">
                            </div>
                            <% } else { %>
                                <div
                                    style="text-align: center; padding: 3rem; background: #f9f9f9; border-radius: 8px;">
                                    <i class="fas fa-file-alt"
                                        style="font-size: 3rem; color: #ccc; margin-bottom: 1rem;"></i>
                                    <p>미리보기를 지원하지 않는 형식입니다.</p>
                                    <a href="<%= bulletin.file %>" target="_blank" class="btn btn-primary">다운로드</a>
                                </div>
                                <% } %>

                                    <div style="margin-top: 2rem; text-align: right;">
                                        <a href="<%= bulletin.file %>" target="_blank" class="btn btn-outline">
                                            <i class="fas fa-external-link-alt"></i> 크게 보기 / 다운로드
                                        </a>
                                    </div>
                </div>

                <div class="related-info-section" data-aos="fade-up">
                    <h4 style="margin-bottom: 2rem; color: var(--color-text-light); font-weight: 600;">다른 나눔 안내 보기</h4>
                    <div class="sharing-info-links">
                        <a href="/sharing/notices" class="btn btn-outline">공지 사항</a>
                        <a href="/sharing/gallery" class="btn btn-outline">교회 앨범</a>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../partials/footer') %>
            <script src="/js/main.js"></script>
</body>

</html>