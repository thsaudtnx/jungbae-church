<!DOCTYPE html>
<html lang="ko">

<head>
  <%- include("../partials/head") %>
</head>

<body>
  <%- include("../partials/header") %>
    <div class="container" style="padding-top: 100px; padding-bottom: 50px;">
      <h1 class="text-center">교회앨범</h1>
      <div class="section-title"></div>

      <% if (locals.user && user.isAdmin) { %>
        <div style="text-align: right; margin-bottom: 1rem;">
          <a href="/admin/write/galleryItems" class="btn btn-primary">앨범 등록</a>
        </div>
        <% } %>

          <div class="card-grid">
            <% if (locals.galleryItems) { %>
              <% galleryItems.forEach(function(item) { const images=Array.isArray(item.images) ? item.images :
                (item.images ? [item.images] : (item.image ? [item.image] : [])); %>
                <div class="card gallery-card" style="padding: 0; overflow: hidden; cursor: pointer;"
                  onclick="openLightbox('<%= item.title %>', '<%= item.date %>', <%- JSON.stringify(images) %>)">
                  <div style="height: 250px; background-color: #eee; overflow: hidden; position: relative;">
                    <% if (images.length> 0) { %>
                      <img src="<%= images[0] %>"
                        style="width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease;"
                        alt="<%= item.title %>">
                      <% if (images.length> 1) { %>
                        <div
                          style="position: absolute; bottom: 12px; right: 12px; background: rgba(0,0,0,0.7); color: white; padding: 5px 10px; border-radius: 20px; font-size: 0.85rem; backdrop-filter: blur(5px);">
                          <i class="fas fa-images"></i> +<%= images.length - 1 %>
                        </div>
                        <% } %>
                          <% } else { %>
                            <div
                              style="width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: #aaa;">
                              <i class="fas fa-image" style="font-size: 3rem;"></i>
                            </div>
                            <% } %>
                  </div>
                  <div style="padding: 1.5rem;">
                    <h3 style="margin-bottom: 0.5rem; font-size: 1.2rem;">
                      <%= item.title %>
                    </h3>
                    <p style="color: #888; margin-bottom: 1rem; font-size: 0.9rem;">
                      <i class="far fa-calendar-alt"></i>
                      <%= item.date %>
                    </p>
                    <% if (locals.user && user.isAdmin) { %>
                      <div style="font-size: 0.85rem; border-top: 1px solid #eee; padding-top: 1rem;"
                        onclick="event.stopPropagation()">
                        <a href="/admin/edit/galleryItems/<%= item.id %>"
                          style="color: var(--color-primary); margin-right: 10px;"><i class="fas fa-edit"></i> 수정</a>
                        <a href="/admin/delete/galleryItems/<%= item.id %>" onclick="return confirm('정말 삭제하시겠습니까?')"
                          style="color: #e74c3c;"><i class="fas fa-trash-alt"></i> 삭제</a>
                      </div>
                      <% } %>
                  </div>
                </div>
                <% }); %>
                  <% } else { %>
                    <p class="text-center" style="grid-column: 1/-1; padding: 5rem 0; color: #999;">등록된 사진이 없습니다.</p>
                    <% } %>
          </div>
    </div>

    <!-- Lightbox Structure -->
    <div id="lightbox" class="lightbox" onclick="closeLightbox()">
      <span class="lightbox-close" onclick="closeLightbox()">&times;</span>
      <div class="lightbox-nav lightbox-prev" onclick="changeImage(-1, event)"><i class="fas fa-chevron-left"></i></div>
      <div class="lightbox-nav lightbox-next" onclick="changeImage(1, event)"><i class="fas fa-chevron-right"></i></div>

      <div class="lightbox-content" onclick="event.stopPropagation()">
        <img id="lightbox-img" class="lightbox-image" src="" alt="확대 이미지">
        <div class="lightbox-info">
          <h4 id="lightbox-title"></h4>
          <p id="lightbox-date"></p>
          <div id="lightbox-counter" class="lightbox-counter"></div>
        </div>
      </div>
    </div>

    <style>
      .gallery-card:hover img {
        transform: scale(1.1);
      }

      .gallery-card {
        transition: all 0.3s ease;
      }

      .gallery-card:hover {
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        transform: translateY(-5px);
      }
    </style>

    <script>
      let currentImages = [];
      let currentIndex = 0;

      function openLightbox(title, date, images) {
        if (!images || images.length === 0) return;

        currentImages = images;
        currentIndex = 0;

        document.getElementById('lightbox-title').innerText = title;
        document.getElementById('lightbox-date').innerText = date;

        updateLightboxImage();

        document.getElementById('lightbox').classList.add('active');
        document.body.style.overflow = 'hidden'; // Scroll lock
      }

      function closeLightbox() {
        document.getElementById('lightbox').classList.remove('active');
        document.body.style.overflow = 'auto';
      }

      function changeImage(step, event) {
        if (event) event.stopPropagation();

        currentIndex += step;
        if (currentIndex >= currentImages.length) currentIndex = 0;
        if (currentIndex < 0) currentIndex = currentImages.length - 1;

        updateLightboxImage();
      }

      function updateLightboxImage() {
        const img = document.getElementById('lightbox-img');
        img.style.opacity = '0';

        setTimeout(() => {
          img.src = currentImages[currentIndex];
          img.onload = () => { img.style.opacity = '1'; };
        }, 50);

        const counter = document.getElementById('lightbox-counter');
        if (currentImages.length > 1) {
          counter.innerText = (currentIndex + 1) + ' / ' + currentImages.length;
          document.querySelectorAll('.lightbox-nav').forEach(nav => nav.style.display = 'flex');
        } else {
          counter.innerText = '';
          document.querySelectorAll('.lightbox-nav').forEach(nav => nav.style.display = 'none');
        }
      }

      // Keyboard controls
      document.addEventListener('keydown', (e) => {
        if (!document.getElementById('lightbox').classList.contains('active')) return;

        if (e.key === 'Escape') closeLightbox();
        if (e.key === 'ArrowLeft') changeImage(-1);
        if (e.key === 'ArrowRight') changeImage(1);
      });
    </script>

    <%- include("../partials/footer") %>
      <script src="/js/main.js"></script>
</body>

</html>