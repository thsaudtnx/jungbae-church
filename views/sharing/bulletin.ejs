<!DOCTYPE html>
<html lang="ko">

<head>
  <%- include("../partials/head") %>
    <style>
      .bulletin-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
      }

      .bulletin-card {
        background: white;
        border-radius: 24px;
        padding: 2rem;
        box-shadow: var(--shadow-soft);
        border: 1px solid var(--glass-border);
        transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        text-decoration: none;
        color: inherit;
        position: relative;
        overflow: hidden;
      }

      .bulletin-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-medium);
        border-color: var(--color-primary);
      }

      .bulletin-icon {
        width: 70px;
        height: 70px;
        background: #fff5f5;
        color: #e74c3c;
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        margin-bottom: 1.5rem;
        transition: all 0.3s ease;
      }

      .bulletin-card:hover .bulletin-icon {
        background: #e74c3c;
        color: white;
        transform: rotate(-10deg) scale(1.1);
      }

      .bulletin-title {
        font-size: 1.25rem;
        font-weight: 800;
        color: var(--color-text);
        margin-bottom: 0.8rem;
        line-height: 1.4;
        word-break: keep-all;
      }

      .bulletin-date {
        font-size: 0.95rem;
        color: var(--color-text-light);
        display: flex;
        align-items: center;
        gap: 6px;
        margin-bottom: 1.5rem;
      }

      .download-badge {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 18px;
        background: var(--color-bg-alt);
        color: var(--color-primary);
        border-radius: 50px;
        font-size: 0.9rem;
        font-weight: 700;
        transition: all 0.3s ease;
      }

      .bulletin-card:hover .download-badge {
        background: var(--color-primary);
        color: white;
      }

      .admin-controls {
        position: absolute;
        top: 15px;
        right: 15px;
        display: flex;
        gap: 8px;
        opacity: 0;
        transition: opacity 0.3s ease;
      }

      .bulletin-card:hover .admin-controls {
        opacity: 1;
      }

      .admin-btn {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: white;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        font-size: 0.8rem;
        transition: all 0.2s;
      }

      .admin-btn:hover {
        transform: scale(1.1);
      }

      @media (max-width: 768px) {
        .bulletin-grid {
          grid-template-columns: 1fr;
          gap: 1.5rem;
        }

        .bulletin-card {
          padding: 1.5rem;
        }
      }
    </style>
</head>

<body>
  <%- include("../partials/header") %>
    <section class="page-header" data-aos="fade">
      <div class="container">
        <h1 data-aos="fade-up" data-aos-delay="100">주보 안내</h1>
        <ul class="breadcrumb" data-aos="fade-up" data-aos-delay="300">
          <li><a href="/">홈</a></li>
          <li>나눔</li>
          <li class="active">주보 안내</li>
        </ul>
      </div>
    </section>

    <div class="container" style="padding-bottom: var(--spacing-xl);">
      <% if (locals.user && user.isAdmin) { %>
        <div style="text-align: right; margin-bottom: 1rem;">
          <a href="#" onclick="openAdminModal('/admin/write/bulletins', '주보 등록')" class="btn btn-primary">주보 등록</a>
        </div>
        <% } %>

          <div class="bulletin-grid">
            <% if (locals.bulletins && bulletins.length> 0) { %>
              <% bulletins.forEach(function(item) { %>
                <div data-aos="fade-up" style="position: relative;">
                  <a href="/sharing/bulletin/<%= item.id %>" class="bulletin-card">
                    <div class="bulletin-icon">
                      <i class="fas fa-file-pdf"></i>
                    </div>
                    <h3 class="bulletin-title">
                      <%= item.title %>
                    </h3>
                    <div class="bulletin-date">
                      <i class="far fa-calendar-alt"></i>
                      <%= item.date %>
                    </div>
                    <div class="download-badge">
                      보기 / 다운로드 <i class="fas fa-arrow-down" style="font-size: 0.8rem;"></i>
                    </div>
                  </a>

                  <% if (locals.user && user.isAdmin) { %>
                    <div class="admin-controls">
                      <a href="#" onclick="openAdminModal('/admin/edit/bulletins/<%= item.id %>', '주보 수정')"
                        class="admin-btn" style="color: var(--color-secondary);">
                        <i class="fas fa-edit"></i>
                      </a>
                      <a href="/admin/delete/bulletins/<%= item.id %>" onclick="return confirm('정말 삭제하시겠습니까?')"
                        class="admin-btn" style="color: #e74c3c;">
                        <i class="fas fa-trash-alt"></i>
                      </a>
                    </div>
                    <% } %>
                </div>
                <% }); %>
                  <% } else { %>
                    <div
                      style="grid-column: 1 / -1; text-align: center; padding: 10rem 0; color: #94a3b8; background: white; border-radius: 30px; border: 1px dashed #cbd5e1;">
                      <i class="fas fa-file-invoice"
                        style="font-size: 4rem; margin-bottom: 2rem; display: block; opacity: 0.3;"></i>
                      <p style="font-size: 1.2rem;">등록된 주보가 없습니다.</p>
                    </div>
                    <% } %>
          </div>

          <% if (locals.totalPages> 1) { %>
            <div class="premium-pagination">
              <% if (currentPage> 1) { %>
                <a href="/sharing/bulletin?page=<%= currentPage - 1 %>" class="nav-btn">
                  <i class="fas fa-chevron-left"></i>
                </a>
                <% } %>

                  <% for (let i=1; i <=totalPages; i++) { %>
                    <% if (i===currentPage) { %>
                      <span class="active">
                        <%= i %>
                      </span>
                      <% } else { %>
                        <% if (i===1 || i===totalPages || (i>= currentPage - 2 && i <= currentPage + 2)) { %>
                            <a href="/sharing/bulletin?page=<%= i %>">
                              <%= i %>
                            </a>
                            <% } else if (i===currentPage - 3 || i===currentPage + 3) { %>
                              <span style="border:none; box-shadow:none; background:none; min-width:20px;">...</span>
                              <% } %>
                                <% } %>
                                  <% } %>

                                    <% if (currentPage < totalPages) { %>
                                      <a href="/sharing/bulletin?page=<%= currentPage + 1 %>" class="nav-btn">
                                        <i class="fas fa-chevron-right"></i>
                                      </a>
                                      <% } %>
            </div>
            <% } %>

              <!-- Related Action -->
              <div class="related-info-section" data-aos="fade-up" style="margin-top: 7rem;">
                <h4 style="margin-bottom: 2.5rem; color: var(--color-text-light); font-weight: 700; font-size: 1.3rem;">
                  다른 나눔 안내 보기</h4>
                <div class="sharing-info-links">
                  <a href="/sharing/notices" class="btn btn-outline" style="border-radius: 15px; padding: 12px 25px;">공지
                    사항</a>
                  <a href="/sharing/gallery" class="btn btn-outline" style="border-radius: 15px; padding: 12px 25px;">교회
                    앨범</a>
                </div>
              </div>
    </div>

    <%- include("../partials/footer") %>
      <script src="/js/main.js"></script>
</body>

</html>