<!DOCTYPE html>
<html lang="ko">

<head>
  <%- include("../partials/head") %>
    <style>
      .board-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 2rem;
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
      }

      .board-table th {
        background: #f8f9fa;
        padding: 1.2rem;
        font-weight: 600;
        color: var(--color-text);
        border-bottom: 2px solid #edf2f7;
        text-align: left;
      }

      .board-table td {
        padding: 1.2rem;
        border-bottom: 1px solid #edf2f7;
        vertical-align: middle;
      }

      .board-table tr:hover {
        background-color: #fcfdfe;
      }

      .date-badge {
        font-size: 0.85rem;
        color: var(--color-text-light);
        background: #f1f3f5;
        padding: 4px 10px;
        border-radius: 20px;
      }

      .board-title {
        font-weight: 600;
        color: var(--color-primary);
        font-size: 1.05rem;
        display: block;
        margin-bottom: 4px;
        text-decoration: none;
      }

      .board-meta {
        font-size: 0.9rem;
        color: #666;
        display: -webkit-box;
        -webkit-line-clamp: 1;
        line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
      }

      @media (max-width: 600px) {

        .board-table th:nth-child(2),
        .board-table td:nth-child(2) {
          display: none;
        }
      }

      .pagination {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.5rem;
        margin-top: 2rem;
        padding: 1rem 0;
      }

      .pagination a,
      .pagination span {
        padding: 0.5rem 1rem;
        border: 1px solid #ddd;
        border-radius: 6px;
        text-decoration: none;
        color: var(--color-text);
        transition: all 0.3s ease;
        min-width: 40px;
        text-align: center;
      }

      .pagination a:hover {
        background: var(--color-primary);
        color: white;
        border-color: var(--color-primary);
      }

      .pagination .active {
        background: var(--color-primary);
        color: white;
        border-color: var(--color-primary);
        font-weight: 600;
      }

      .pagination .disabled {
        opacity: 0.5;
        cursor: not-allowed;
        pointer-events: none;
      }
    </style>
</head>

<body>
  <%- include("../partials/header") %>
    <div class="container" style="padding-top: 100px; padding-bottom: 50px;">
      <h1 class="text-center">공지사항</h1>
      <div class="section-title"></div>

      <% if (locals.user && user.isAdmin) { %>
        <div style="text-align: right; margin-bottom: 1rem;">
          <a href="/admin/write/notices" class="btn btn-primary">공지 등록</a>
        </div>
        <% } %>

          <table class="board-table transition-all">
            <thead>
              <tr>
                <th style="width: 60%;">공지 정보</th>
                <th style="width: 25%;">날짜</th>
                <% if (locals.user && user.isAdmin) { %>
                  <th style="width: 15%; text-align: center;">관리</th>
                  <% } %>
              </tr>
            </thead>
            <tbody>
              <% if (locals.notices && notices.length> 0) { %>
                <% notices.forEach(function(item) { %>
                  <tr>
                    <td>
                      <a href="/sharing/notices/<%= item.id %>" class="board-title">
                        <%= item.title %>
                      </a>
                      <% if (item.content) { %>
                        <div class="board-meta">
                          <%= item.content.substring(0, 100) %>...
                        </div>
                        <% } %>
                    </td>
                    <td>
                      <span class="date-badge">
                        <%= item.date %>
                      </span>
                    </td>
                    <% if (locals.user && user.isAdmin) { %>
                      <td style="text-align: center; font-size: 0.85rem;">
                        <a href="/admin/edit/notices/<%= item.id %>"
                          style="color: var(--color-secondary); margin-right: 8px;"><i class="fas fa-edit"></i></a>
                        <a href="/admin/delete/notices/<%= item.id %>" onclick="return confirm('정말 삭제하시겠습니까?')"
                          style="color: #e74c3c;"><i class="fas fa-trash"></i></a>
                      </td>
                      <% } %>
                  </tr>
                  <% }); %>
                    <% } else { %>
                      <tr>
                        <td colspan="<%= (locals.user && user.isAdmin) ? 3 : 2 %>" class="text-center"
                          style="padding: 3rem; color: #aaa;">
                          등록된 공지가 없습니다.
                        </td>
                      </tr>
                      <% } %>
            </tbody>
          </table>

          <% if (totalPages> 1) { %>
            <div class="pagination">
              <% if (currentPage> 1) { %>
                <a href="/sharing/notices?page=<%= currentPage - 1 %>">
                  <i class="fas fa-chevron-left"></i> 이전
                </a>
                <% } %>

                  <% for (let i=1; i <=totalPages; i++) { %>
                    <% if (i===currentPage) { %>
                      <span class="active">
                        <%= i %>
                      </span>
                      <% } else { %>
                        <a href="/sharing/notices?page=<%= i %>">
                          <%= i %>
                        </a>
                        <% } %>
                          <% } %>

                            <% if (currentPage < totalPages) { %>
                              <a href="/sharing/notices?page=<%= currentPage + 1 %>">
                                다음 <i class="fas fa-chevron-right"></i>
                              </a>
                              <% } %>
            </div>
            <% } %>
    </div>
    <%- include("../partials/footer") %>
      <script src="/js/main.js"></script>
</body>

</html>