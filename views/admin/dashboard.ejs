<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('../partials/head') %>
        <style>
            .dashboard-container {
                padding-top: 120px;
                padding-bottom: 50px;
                min-height: 100vh;
                background: var(--color-bg-alt);
            }

            .dashboard-header {
                background: white;
                padding: 2rem;
                border-radius: 15px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                margin-bottom: 2rem;
            }

            .stats-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                gap: 1.5rem;
                margin-bottom: 3rem;
            }

            .stat-card {
                background: white;
                padding: 1.5rem;
                border-radius: 10px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                text-align: center;
                transition: all 0.3s ease;
            }

            .stat-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            }

            .stat-card i {
                font-size: 2.5rem;
                margin-bottom: 1rem;
            }

            .stat-card h3 {
                font-size: 2rem;
                color: var(--color-text);
                margin-bottom: 0.5rem;
            }

            .stat-card p {
                color: var(--color-text-light);
                font-size: 0.9rem;
            }

            .quick-actions {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 1rem;
                margin-bottom: 3rem;
            }

            .quick-action-btn {
                display: flex;
                align-items: center;
                gap: 1rem;
                padding: 1rem 1.5rem;
                background: white;
                border-radius: 10px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                text-decoration: none;
                color: var(--color-text);
                transition: all 0.3s ease;
            }

            .quick-action-btn:hover {
                transform: translateX(5px);
                box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            }

            .quick-action-btn i {
                font-size: 1.5rem;
                color: var(--color-primary);
            }

            .recent-section {
                background: white;
                padding: 2rem;
                border-radius: 15px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                margin-bottom: 2rem;
            }

            .recent-section h2 {
                font-size: 1.5rem;
                margin-bottom: 1.5rem;
                color: var(--color-text);
                display: flex;
                align-items: center;
                gap: 10px;
            }

            .recent-list {
                list-style: none;
            }

            .recent-item {
                padding: 1rem;
                border-bottom: 1px solid #f5f5f5;
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            .recent-item:last-child {
                border-bottom: none;
            }

            .recent-item:hover {
                background: var(--color-bg-alt);
            }

            .recent-item-title {
                font-weight: 500;
                color: var(--color-text);
            }

            .recent-item-date {
                font-size: 0.85rem;
                color: var(--color-text-light);
            }
        </style>
</head>

<body>
    <%- include('../partials/header') %>

        <div class="container dashboard-container">
            <div class="dashboard-header">
                <h1 style="margin-bottom: 0.5rem;">
                    <i class="fas fa-tachometer-alt" style="color: var(--color-primary);"></i>
                    관리자 대시보드
                </h1>
                <p style="color: var(--color-text-light);">정배교회 콘텐츠 관리</p>
            </div>

            <!-- Statistics -->
            <div class="stats-grid">
                <div class="stat-card">
                    <i class="fas fa-bible" style="color: var(--color-primary);"></i>
                    <h3>
                        <%= stats.sermons %>
                    </h3>
                    <p>주일설교</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-book-open" style="color: var(--color-secondary);"></i>
                    <h3>
                        <%= stats.meditations %>
                    </h3>
                    <p>새벽묵상</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-pen-fancy" style="color: #9b59b6;"></i>
                    <h3>
                        <%= stats.diaries %>
                    </h3>
                    <p>목양일기</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-bullhorn" style="color: #e74c3c;"></i>
                    <h3>
                        <%= stats.notices %>
                    </h3>
                    <p>공지사항</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-file-alt" style="color: #f39c12;"></i>
                    <h3>
                        <%= stats.bulletins %>
                    </h3>
                    <p>주보</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-images" style="color: #16a085;"></i>
                    <h3>
                        <%= stats.gallery %>
                    </h3>
                    <p>교회앨범</p>
                </div>
            </div>

            <!-- Quick Actions -->
            <h2 style="margin-bottom: 1rem; color: var(--color-text);">
                <i class="fas fa-bolt" style="color: var(--color-accent);"></i> 빠른 작성
            </h2>
            <div class="quick-actions">
                <a href="/admin/write/sermons" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>설교 등록</span>
                </a>
                <a href="/admin/write/meditations" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>묵상 등록</span>
                </a>
                <a href="/admin/write/diaries" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>일기 등록</span>
                </a>
                <a href="/admin/write/notices" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>공지 등록</span>
                </a>
                <a href="/admin/write/bulletins" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>주보 등록</span>
                </a>
                <a href="/admin/write/galleryItems" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>앨범 등록</span>
                </a>
                <a href="/admin/write/worshipGuides" class="quick-action-btn">
                    <i class="fas fa-plus-circle"></i>
                    <span>예배 안내 등록</span>
                </a>
            </div>

            <!-- Recent Content -->
            <div class="recent-section">
                <h2>
                    <i class="fas fa-clock"></i> 최근 등록된 설교
                </h2>
                <% if (recentContent.sermons && recentContent.sermons.length> 0) { %>
                    <ul class="recent-list">
                        <% recentContent.sermons.forEach(function(sermon) { %>
                            <li class="recent-item">
                                <div>
                                    <div class="recent-item-title">
                                        <%= sermon.title %>
                                    </div>
                                    <div class="recent-item-date">
                                        <i class="far fa-calendar-alt"></i>
                                        <%= sermon.date %> |
                                            <i class="far fa-user"></i>
                                            <%= sermon.preacher %>
                                    </div>
                                </div>
                                <a href="/admin/edit/sermons/<%= sermon.id %>" class="btn btn-outline"
                                    style="padding: 0.5rem 1rem;">
                                    <i class="fas fa-edit"></i> 수정
                                </a>
                            </li>
                            <% }); %>
                    </ul>
                    <% } else { %>
                        <p style="text-align: center; color: var(--color-text-light); padding: 2rem;">등록된 설교가 없습니다.</p>
                        <% } %>
            </div>

            <div class="recent-section">
                <h2>
                    <i class="fas fa-clock"></i> 최근 공지사항
                </h2>
                <% if (recentContent.notices && recentContent.notices.length> 0) { %>
                    <ul class="recent-list">
                        <% recentContent.notices.forEach(function(notice) { %>
                            <li class="recent-item">
                                <div>
                                    <div class="recent-item-title">
                                        <%= notice.title %>
                                    </div>
                                    <div class="recent-item-date">
                                        <i class="far fa-calendar-alt"></i>
                                        <%= notice.date %>
                                    </div>
                                </div>
                                <a href="/admin/edit/notices/<%= notice.id %>" class="btn btn-outline"
                                    style="padding: 0.5rem 1rem;">
                                    <i class="fas fa-edit"></i> 수정
                                </a>
                            </li>
                            <% }); %>
                    </ul>
                    <% } else { %>
                        <p style="text-align: center; color: var(--color-text-light); padding: 2rem;">등록된 공지사항이 없습니다.
                        </p>
                        <% } %>
            </div>
        </div>

        <%- include('../partials/footer') %>
            <script src="/js/main.js"></script>
</body>

</html>