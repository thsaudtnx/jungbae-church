<!DOCTYPE html>
<html lang="ko">

<head>
  <%- include("../partials/head") %>
    <style>
      .location-grid {
        display: grid;
        grid-template-columns: 1fr 380px;
        gap: 3rem;
        margin-bottom: 5rem;
      }

      .map-section {
        position: relative;
      }

      .map-wrapper {
        background: white;
        padding: 12px;
        border-radius: 40px;
        box-shadow: var(--shadow-medium);
        border: 1px solid var(--glass-border);
        height: 650px;
        overflow: hidden;
        position: sticky;
        top: calc(var(--header-height) + 30px);
      }

      .map-wrapper img,
      .map-wrapper iframe {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 30px;
      }

      .info-sidebar {
        display: flex;
        flex-direction: column;
        gap: 2rem;
      }

      .location-card {
        background: white;
        padding: 2.5rem;
        border-radius: 35px;
        box-shadow: var(--shadow-soft);
        border: 1px solid var(--glass-border);
        transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
      }

      .location-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-medium);
        border-color: rgba(0, 69, 140, 0.1);
      }

      .card-icon {
        width: 56px;
        height: 56px;
        background: rgba(0, 69, 140, 0.05);
        color: var(--color-primary);
        border-radius: 18px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        margin-bottom: 1.8rem;
      }

      .card-title {
        font-size: 1.4rem;
        font-weight: 800;
        color: var(--color-text);
        margin-bottom: 1.2rem;
        letter-spacing: -0.02em;
      }

      .address-text {
        font-size: 1.15rem;
        line-height: 1.7;
        color: var(--color-text);
        margin-bottom: 1.8rem;
        word-break: keep-all;
      }

      .map-shortcuts {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 12px;
        margin-top: 1.5rem;
      }

      .shortcut-btn {
        padding: 14px;
        border-radius: 18px;
        font-size: 0.95rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        transition: all 0.3s ease;
      }

      .btn-naver {
        background: #03C75A;
        color: white;
      }

      .btn-kakao {
        background: #FFCD00;
        color: #3C1E1E;
      }

      .transport-item {
        display: flex;
        gap: 15px;
        margin-bottom: 1.5rem;
      }

      .transport-item:last-child {
        margin-bottom: 0;
      }

      .transport-icon {
        color: var(--color-primary);
        font-size: 1.1rem;
        margin-top: 3px;
      }

      .transport-content h5 {
        font-size: 1rem;
        font-weight: 700;
        margin-bottom: 0.4rem;
        color: var(--color-text);
      }

      .transport-content p {
        font-size: 0.95rem;
        color: var(--color-text-light);
        line-height: 1.6;
      }

      @media (max-width: 1200px) {
        .location-grid {
          grid-template-columns: 1fr;
        }

        .map-wrapper {
          height: 500px;
          position: static;
        }

        .info-sidebar {
          display: grid;
          grid-template-columns: 1fr 1fr;
        }
      }

      @media (max-width: 768px) {
        .info-sidebar {
          grid-template-columns: 1fr;
        }

        .location-card {
          padding: 2rem;
        }
      }
    </style>
</head>

<body>
  <%- include("../partials/header") %>

    <section class="page-header" data-aos="fade">
      <div class="container">
        <h1 data-aos="fade-up" data-aos-delay="100">오시는 길</h1>
        <ul class="breadcrumb" data-aos="fade-up" data-aos-delay="300">
          <li><a href="/">홈</a></li>
          <li>교회</li>
          <li class="active">오시는 길</li>
        </ul>
      </div>
    </section>

    <div class="container" style="padding-bottom: var(--spacing-xl);">
      <div class="location-grid">
        <!-- Map Section -->
        <div class="map-section" data-aos="fade-right">
          <div class="map-wrapper">
            <img src="/images/map.png" alt="정배교회 오시는 길 안내도" />
          </div>
        </div>

        <!-- Info Sidebar -->
        <div class="info-sidebar">
          <!-- Address Card -->
          <div class="location-card" data-aos="fade-left" data-aos-delay="200">
            <div class="card-icon"><i class="fas fa-map-marker-alt"></i></div>
            <div class="card-title">교회 위치 안내</div>
            <p class="address-text">
              경기도 양평군 서종면<br>
              <strong>중미산로 773</strong><br>
              <span style="font-size: 0.9rem; color: var(--color-text-light);">(지번: 정배리 401-2)</span>
            </p>

            <button onclick="copyAddress()" class="btn btn-outline"
              style="width: 100%; justify-content: center; margin-bottom: 1.2rem; border-radius: 18px;">
              <i class="far fa-copy" style="margin-right: 8px;"></i> 주소 복사하기
            </button>

            <div class="map-shortcuts">
              <a href="https://naver.me/5jBJuuQh" target="_blank" class="shortcut-btn btn-naver">
                네이버 지도
              </a>
              <a href="https://map.kakao.com/link/search/경기도 양평군 서종면 중미산로 773" target="_blank"
                class="shortcut-btn btn-kakao">
                카카오맵
              </a>
            </div>
          </div>


        </div>
      </div>

      <!-- Related Action -->
      <div class="related-info-section" data-aos="fade-up">
        <h4 style="margin-bottom: 2rem; color: var(--color-text-light); font-weight: 600;">다른 교회 안내 보기</h4>
        <div class="church-info-links">
          <a href="/church/philosophy" class="btn btn-outline">교회 소개</a>
          <a href="/church/history" class="btn btn-outline">교회 연혁</a>
          <a href="/church/pastor" class="btn btn-outline">담임목사 소개</a>
          <a href="/church/worship" class="btn btn-outline">예배 안내</a>
        </div>
      </div>
    </div>

    <script>
      function copyAddress() {
        const addr = '경기도 양평군 서종면 중미산로 773';
        navigator.clipboard.writeText(addr).then(() => {
          alert('주소가 성공적으로 복사되었습니다.');
        });
      }
    </script>

    <%- include("../partials/footer") %>
      <script src="/js/main.js"></script>
</body>

</html>